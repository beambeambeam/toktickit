glob_matcher: doublestar

pre-commit:
  parallel: false
  commands:
    oxfmt:
      priority: 1
      glob: "**/*.{js,jsx,ts,tsx,json,jsonc,css,scss,md,mdx}"
      run: pnpm exec oxfmt --config oxfmt.config.ts --write "{staged_files}"
      stage_fixed: true

    oxlint:
      priority: 2
      glob: "**/*.{js,jsx,ts,tsx}"
      exclude:
        - "client/src/**/routeTree.gen.ts"
      run: pnpm exec oxlint --config oxlint.config.ts --fix "{staged_files}"
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit "{1}"
